name: test-driven-development-eval
description: |
  Evaluate the RED-GREEN-REFACTOR workflow on a small local behavior change and
  a request to skip the test-first step.
skill: test-driven-development
schemaVersion: "1.2"
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  executor: copilot-sdk
  model: gpt-4.1
tasks:
  - "tasks/*.yaml"
