name: security-review-eval
description: |
  Evaluate a read-only local security review, evidence-backed findings, and
  filtering of benign look-alikes.
skill: security-review
schemaVersion: "1.2"
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  executor: copilot-sdk
  model: gpt-4.1
tasks:
  - "tasks/*.yaml"
