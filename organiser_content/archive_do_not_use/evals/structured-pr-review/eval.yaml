name: structured-pr-review-eval
description: |
  Evaluate independent local pull-request review with prioritized findings,
  questions, line evidence, and no approval or edit side effects.
skill: structured-pr-review
schemaVersion: "1.2"
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  executor: copilot-sdk
  model: gpt-4.1
tasks:
  - "tasks/*.yaml"
