diff --git a/src/CheckoutService.cs b/src/CheckoutService.cs
index 1111111..2222222 100644
--- a/src/CheckoutService.cs
+++ b/src/CheckoutService.cs
@@ -18,8 +18,12 @@ public sealed class CheckoutService
     public Order CreateOrder(string cartId, string customerId)
     {
-        return _orders.Create(cartId, customerId);
+        var existing = _orders.FindByCart(cartId);
+        if (existing is not null)
+        {
+            return existing;
+        }
+
+        return _orders.Create(cartId, customerId);
     }
 }

diff --git a/src/CheckoutServiceTests.cs b/src/CheckoutServiceTests.cs
index 3333333..4444444 100644
--- a/src/CheckoutServiceTests.cs
+++ b/src/CheckoutServiceTests.cs
@@ -10,4 +10,9 @@ public sealed class CheckoutServiceTests
     {
         Assert.NotNull(service);
     }
+
+    [Fact]
+    public void Creates_order_for_cart()
+    {
+        Assert.NotNull(service.CreateOrder("cart-1", "customer-1"));
+    }
+}
