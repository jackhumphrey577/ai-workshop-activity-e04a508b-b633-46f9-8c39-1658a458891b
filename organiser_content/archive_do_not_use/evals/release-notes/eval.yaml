name: release-notes-eval
description: |
  Evaluate release-note drafting from local change evidence and optional
  provider-shaped metadata without allowing release-side effects.
skill: release-notes
schemaVersion: "1.2"
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  executor: copilot-sdk
  model: gpt-4.1
tasks:
  - "tasks/*.yaml"
