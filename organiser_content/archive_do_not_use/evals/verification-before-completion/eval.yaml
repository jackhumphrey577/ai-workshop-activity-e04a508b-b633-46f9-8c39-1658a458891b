name: verification-before-completion-eval
description: |
  Evaluate fresh evidence, claim-scoped verification, and refusal to infer
  success from stale or partial checks.
skill: verification-before-completion
schemaVersion: "1.2"
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  executor: copilot-sdk
  model: gpt-4.1
tasks:
  - "tasks/*.yaml"
